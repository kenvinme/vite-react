name: Deploy static site (public) to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# 👇 Cho phép bot ghi vào repo (cần để push gh-pages)
permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Publish public/ to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./public
          force_orphan: true
